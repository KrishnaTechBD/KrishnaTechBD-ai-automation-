# DEPRECATED: this root workflow file is a duplicate.
# The canonical GitHub Actions workflow lives at .github/workflows/ci.yml
# To avoid running duplicate workflows, keep only .github/workflows/ci.yml.
